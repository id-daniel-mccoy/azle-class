{
    "scripts": {
        "pretest": "tsx test/pretest.ts",
        "test": "jest",
        "build": "dfx build",
        "setup": "npm install && dfx start --background && dfx canister create --all && dfx build && dfx canister install --all"
    },
    "dependencies": {
        "azle": "^0.24.1"
    },
    "devDependencies": {
        "jest": "^29.7.0",
        "ts-jest": "^29.1.4",
        "tsx": "^4.15.7",
        "typescript": "^5.2.2"
    }
}