{
    "scripts": {
        "setup": "npm install && cd hello && npm run setup && cd ../frontend && npm run setup"
    },
    "dependencies": {
        "azle": "^0.24.1"
    },
    "devDependencies": {
        "jest": "^29.7.0",
        "ts-jest": "^29.1.4",
        "tsx": "^4.15.7",
        "typescript": "^5.2.2"
    }
}